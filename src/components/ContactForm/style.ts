import { FormHTMLAttributes } from 'react';
import styled from 'styled-components';

export const Container = styled.form<FormHTMLAttributes<HTMLFormElement>>``;

export const ButtonContainer = styled.div`
  margin-top: 24px;
`;
